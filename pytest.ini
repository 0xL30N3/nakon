[pytest]
markers =
    proxmox: requires live Proxmox infrastructure and credentials (see tests/proxmox/README.md); excluded by default
# The proxmox tier clones real VMs and costs minutes per run, so it is opt-in:
#   python3 -m pytest -m proxmox
addopts = -m "not proxmox" -ra
testpaths = tests
